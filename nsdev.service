[Unit]
Description=NSDev service
After=multi-user.target network.target
[Service]
Type=simple
EnvironmentFile=/etc/nsdev-daemon.cfg
ExecStart=/bin/bash -a -c '${DAEMON_EXE} ${DAEMON_OPTIONS}'
Restart=always
RestartSec=3
[Install]
WantedBy=multi-user.target
